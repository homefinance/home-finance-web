describe('RestService', function() {

    beforeEach(module('homeFinanceWeb'));

    it('should ...', inject(function(RestService) {

        //expect(RestService.doSomething()).toEqual('something');

    }));

});